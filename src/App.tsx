import {useEffect,useMemo,useState} from 'react'
import './styles.css'
import type {Filters,Ticket} from './types/support'
import {metrics,parseCSV,signal} from './lib/analytics'
import {CategoryBars,FilterBar,HealthTable,KPIGrid} from './components/Dashboard'

export default function App(){const[rows,setRows]=useState<Ticket[]>([]);const[filters,setFilters]=useState<Filters>({category:'All',channel:'All',priority:'All'});useEffect(()=>{fetch('/data/support_tickets.csv').then(r=>r.text()).then(x=>setRows(parseCSV(x))).catch(()=>setRows([]))},[]);const selected=useMemo(()=>rows.filter(x=>(filters.category==='All'||x.category===filters.category)&&(filters.channel==='All'||x.channel===filters.channel)&&(filters.priority==='All'||x.priority===filters.priority)),[rows,filters]);const m=metrics(selected),s=signal(selected);return <><header><div className="nav"><b>SO<span>/</span>Analytics</b><a href="https://github.com/sofialozano-cx/support-operations-analytics">Source ↗</a></div><div className="hero"><p className="eyebrow">Support Operations · Interactive Case Study</p><h1>Turn support data into<br/><em>operational clarity.</em></h1><p className="lead">Explore a fictional SaaS support operation through the metrics that shape service reliability, escalation and customer experience.</p><div className="meta"><span>60 synthetic tickets</span><span>July 2026</span><span>No customer data</span></div></div></header><main><section className="control"><div><p className="eyebrow">Explore the operation</p><h2>Segment the support queue</h2></div><FilterBar rows={rows} value={filters} onChange={setFilters}/></section><KPIGrid m={m}/><section className="layout"><article className="panel wide"><div className="panelHead"><div><p className="eyebrow">Distribution</p><h2>Ticket volume</h2></div><span>by category</span></div><CategoryBars rows={selected} kind="volume"/></article><article className={`signal ${s.level}`}><p className="eyebrow">Operational signal</p><h2>{s.title}</h2><p>{s.body}</p><div className="chips"><span>SLA</span><span>Escalation</span><span>Resolution</span><span>CSAT</span></div></article></section><section className="layout"><article className="panel"><div className="panelHead"><div><p className="eyebrow">Reliability</p><h2>SLA compliance</h2></div><span>by category</span></div><CategoryBars rows={selected} kind="sla"/></article><article className="panel"><div className="panelHead"><div><p className="eyebrow">Queue comparison</p><h2>Operational health</h2></div></div><HealthTable rows={selected}/></article></section><section className="method"><p className="eyebrow">Methodology</p><h2>Metrics are signals, not conclusions.</h2><p>This portfolio uses synthetic data. Operational signals combine SLA, escalation and resolution patterns to prioritize investigation; they do not establish causation. Small filtered samples should be interpreted cautiously.</p></section></main><footer><b>Sofia Lozano</b><span>Customer Experience · Technical Support · CRM & Support Operations</span><a href="https://github.com/sofialozano-cx">GitHub ↗</a></footer></>}
